# `stop` — Stop Running Timer

Stops the currently running timer. No arguments needed — it auto-detects the running entry.

## Usage

```bash
npm run stop
```

## Output

With a running timer:
```
Stopped: Fixing login bug [Dev-Pilot] (1h15m)
```

Without a running timer:
```
No running timer.
```
